name: Build & Test

on:
  push:
    branches: ["**"]
    tags: ["**"]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    strategy:
      matrix:
        php_version: ["8.2", "8.3", "8.4"]
        laravel_version: ["10.0", "11.0", "12.0", "13.0"]
        exclude:
          # Laravel 13 requires PHP >= 8.3
          - php_version: "8.2"
            laravel_version: "13.0"

    runs-on: ubuntu-latest
    container: "php:${{ matrix.php_version }}-alpine"
    steps:
    - uses: actions/checkout@v3

    - name: php version
      run: php -v

    - name: Install Composer
      run: |
        curl https://getcomposer.org/download/latest-stable/composer.phar -o composer.phar
        chmod a+x composer.phar

    - name: Update composer to laravel version
      run: ./composer.phar require "illuminate/support:^${{ matrix.laravel_version }}"

    - name: Validate composer.json and composer.lock
      run: ./composer.phar validate --strict

    - name: Cache Composer packages
      id: composer-cache
      uses: actions/cache@v3
      with:
        path: vendor
        key: ${{ runner.os }}-php-${{ matrix.laravel_version }}-${{ hashFiles('**/composer.lock') }}
        restore-keys: |
          ${{ runner.os }}-php-

    - name: Install dependencies
      run: ./composer.phar install --prefer-dist --no-progress

    - name: Run test suite
      run: ./composer.phar run-script test

  publish:
    needs: build
    runs-on: ubuntu-latest
    if: ${{ startsWith(github.ref, 'refs/tags') }}
    steps:
    - name: Notify Packagist
      run: |
        curl -XPOST -H'content-type:application/json' 'https://packagist.org/api/update-package?username=samyapp&apiToken=${{ secrets.PACKAGIST_API_TOKEN }}' \
          -d'{"repository":{"url":"https://packagist.org/packages/samyapp/laravel-external-authentication"}}'